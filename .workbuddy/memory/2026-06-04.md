# 2026-06-04 工作日志

## Astro Starlight 技术博客 — 两级导航架构

### 完成事项
- 创建 Java/Rust/Go/Python 四个语言的内容目录和文章（overview, basics, frameworks, advanced）
- 实现**一级导航（顶部）+ 二级导航（左侧栏）** 的两级导航架构
- 覆盖 `Header.astro`：在站点标题右侧添加语言 pill 标签（Java/Rust/Go/Python）
- 覆盖 `Sidebar.astro`：根据当前 URL 路径过滤侧边栏，只显示当前语言的二级分类
- Sidebar 使用 `entry.label.toLowerCase()` 匹配路径前缀来过滤顶层 Group

### 关键技术决策
- Starlight sidebar 只支持数组配置，不支持路径过滤 → 在 Sidebar.astro 渲染层做过滤
- Header 无命名 slot → 整体覆盖 Header 组件
- 保留 4 个顶层 Group 在 sidebar 配置中（Starlight 依赖它计算 isCurrent 和分页）
- 首页（`/`）时 sidebar 为空，语言标签无高亮

### 文件清单
- `src/components/overrides/Header.astro` — 顶部导航 + 语言标签
- `src/components/overrides/Sidebar.astro` — 路径过滤 + SidebarSublist 渲染
- `astro.config.mjs` — components 覆盖 + sidebar 分组配置
- `src/content/docs/{java,rust,go,python}/{basics,frameworks,advanced}/*.md` — 内容文件

### 已安装 Astro Docs MCP
- 配置在 `~/.workbuddy/mcp.json`，URL: `https://mcp.docs.astro.build/mcp`
